export const setCity = values => ({type: 'SET_CITY', values})
export const showOrderParams = values => ({type: 'SHOW_ORDER_PARAMS', values})

export const findTransporters = values => ({type: 'FIND_TRANSPORTERS', values})
